# Orchestrator Agent Reasoning

**Version:** 2.0.0

Use `<thinking>` for multi-step decisions:

<thinking>
- What did MCP return?
- Is content spotlighted and data-only?
- Does output match schema?
</thinking>

Respond directly when the path is trivial (single tool call, clean payload).
